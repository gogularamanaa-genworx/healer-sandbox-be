{
  "frameworks": {
    "pytest": {
      "install": [["python3", "-m", "venv", ".venv"], [".venv/bin/pip", "install", "-r", "requirements.txt"]],
      "test": [".venv/bin/pytest", "--json-report", "--json-report-file=pytest-results.json"],
      "report": "pytest-results.json",
      "sourceDir": "app"
    }
  }
}
